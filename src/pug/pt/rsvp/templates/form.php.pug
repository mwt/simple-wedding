doctype html
html(lang='pt').h-100.w-100

    head

        meta(charset='utf-8')
        meta(name='viewport', content='width=device-width, initial-scale=1, shrink-to-fit=no')
        meta(name='description', content='Matthew W. Thomas e Maria Fernanda Petri Betto vão se casar em 26 de julho de 2025 em São Paulo, Brasil. Envie este formulário para RSVP.')
        meta(name='author', content='Matthew W. Thomas e Maria Fernanda Petri Betto')

        title Matthew &amp; Maria Fernanda | RSVP

        link(rel='icon', type='image/svg+xml', href='/assets/img/favicon.svg')
        link(rel='icon', type='image/png', href='/assets/img/favicon.png')

        // Core theme CSS (includes Bootstrap)
        link(href='/css/styles.css', rel='stylesheet')

        style .content {max-width: 500px}
        
    body.h-100.w-100.bg-light.bg-cream-paper
        .d-flex.h-100.w-100.bg-spattern2
            .d-flex.flex-column.justify-content-center.mx-auto
                .content.bg-light.bg-handmade-paper.border.shadow-sm.p-4
                    h1.my-3.text-center RSVP
                    p Você está convidado para o casamento de Matthew Thomas e Maria Betto no dia 26 de julho de 2025 em São Paulo Brasil. Por favor confirme sua presença abaixo.
                    form(action="", method="post", enctype="multipart/form-data", accept-charset="utf-8")
                        <?php foreach ($people as $key => $guest_name) { ?>
                            .my-3.row
                                .form-group.col
                                    <label for="guest[<?= $key ?>][name]" class="form-label">Nome do convidado</label>
                                    <input type="text" id="guest[<?= $key ?>][name]" name="guest[<?= $key ?>][name]" class="form-control" value="<?= $guest_name ?>" <?= $guest_name === "" ? "" : "readonly" ?>>
                                .form-group.col-auto
                                    <label for="guest[<?= $key ?>][attend]" class="form-label">Presença</label>
                                    <select id="guest[<?= $key ?>][attend]" name="guest[<?= $key ?>][attend]" class="form-control">
                                        option Sim
                                        option Não
                                        option Talvez
                                    </select>
                        <?php } ?>
                        <?php if (empty($_GET['e'])) { ?>
                            .my-3.row 
                                .form-group
                                    label.form-label(for="email") Email para contato
                                    input.form-control#email(value="", type="email", name="email" required)
                        <?php } else { ?>
                            <input type="hidden" name="email" value="<?= $_GET['e'] ?>">
                        <?php } ?>
                        button.btn.btn-danger.bg-paper-fibers(type="submit") Enviar!
